apiVersion: v1
kind: Service
metadata:
  labels:
    app: health 
  name: health-service
  namespace: desafiopython
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP    
  type: LoadBalancer
  selector:
    app: health
